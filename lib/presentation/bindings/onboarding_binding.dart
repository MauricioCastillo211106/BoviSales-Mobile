import 'package:get/get.dart';

class OnboardingBinding extends Bindings {
  @override
  void dependencies() {
    // No necesitamos un controlador por ahora.
    // Podemos agregar la inicialización del controlador aquí en el futuro si es necesario.
  }
}
